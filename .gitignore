.DS_Store
.vercel

# ATENCAO — fontes licenciadas (National 2, Klim Type Foundry).
# Os .otf ESTAO versionados por decisao do Rodrigo (2026-09-17), em carater
# temporario, pra conseguir ver o resultado no ar. Enquanto este repositorio
# for PUBLICO, isso e redistribuicao do arquivo-fonte, que a licenca nao
# permite. Tornar o repo privado resolve — e a conta e pessoal (nao uma org),
# entao o plano gratuito da Vercel publica de repo privado normalmente.
